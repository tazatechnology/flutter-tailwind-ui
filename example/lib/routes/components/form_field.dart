part of 'components.dart';

// =============================================================================
// CLASS: ComponentRouteTFormField
// =============================================================================

class ComponentRouteTFormField extends StatelessWidget {
  const ComponentRouteTFormField({super.key});

  @override
  Widget build(BuildContext context) {
    return const ComponentRoute(
      name: 'TFormField',
      description: 'Wrapper for any `FormField` widget.',
      children: [
        AppSection(
          title: 'Section',
          children: [],
        ),
      ],
    );
  }
}
