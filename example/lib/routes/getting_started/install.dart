import 'package:flutter/material.dart';

// =================================================
// CLASS: InstallRoute
// =================================================

class InstallRoute extends StatelessWidget {
  const InstallRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('InstallRoute');
  }
}
