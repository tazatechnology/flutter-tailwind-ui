import 'package:flutter/material.dart';

// =================================================
// CLASS: ColorsRoute
// =================================================

class ColorsRoute extends StatelessWidget {
  const ColorsRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('ColorsRoute');
  }
}
